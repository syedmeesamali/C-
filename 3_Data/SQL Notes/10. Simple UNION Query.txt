(SELECT Prod_ID, Prod_Name, Units, Expiry
FROM StockinTable WHERE Prod_Name = 'TATA TEA 250 G X 48')
UNION ALL
(SELECT Prod_ID, Prod_Name, Boxes, Date
FROM StockoutTable WHERE Prod_Name = 'TATA TEA 250 G X 48')